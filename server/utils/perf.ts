// import { performance } from 'node:perf_hooks';
// import { Logger } from '@/utils';

// const startPerfTimer = (): number => performance.now();

// const endPerfTimer = (): number => performance.now();

// const calculatePerformance = (startTime: number, endTime: number): void => {
//   Logger.info(`Response took ${endTime - startTime} milliseconds`);
// };

// export { startPerfTimer, endPerfTimer, calculatePerformance };
export {};
